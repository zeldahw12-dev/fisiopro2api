# ==========================================
# ? Render & Desarrollo - Configuración base
# ==========================================

spring.application.name=fisio
server.servlet.context-path=/api
server.port=8080

# ==========================================
# ?? Base de datos (Railway / Render)
# ==========================================
spring.datasource.url=${DB_URL:jdbc:mysql://ballast.proxy.rlwy.net:24382/railway}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:ZdcGnmhLDviupieZxkBNTfbXmpwWaScV}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==========================================
# ? Seguridad y JWT
# ==========================================
app.security.permit-all=true
app.jwt.secret=dev-secret-please-change-and-set-env
app.jwt.issuer=

# ==========================================
# ? CORS (para Expo, web y Render)
# ==========================================
app.cors.allowed-origins=*

# ==========================================
# ? Correo (modo log)
# ==========================================
spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=${MAIL_USER:fakeuser}
spring.mail.password=${MAIL_PASS:fakepass}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=FisioApp <no-reply@fisioapp.local>

# ==========================================
# ? Logging
# ==========================================
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=ERROR
logging.level.root=INFO
